# ╔══════════════════════════════════════════════════════════════════╗
# ║ DO NOT EDIT — DECLARATIVE ENVIRONMENT — NIX FLAKES WAY           ║
# ║ AUTO-GENERATED by _shared/engine.nix — dist/ output is a build   ║
# ║ artifact. Edit src/ + run build.sh, never touch dist/.           ║
# ╠══════════════════════════════════════════════════════════════════╣
# ║ Source:  a_solutions/aa-sui_google-workspace-mcp/src/flake.nix
# ║ Rebuild: a_solutions/aa-sui_google-workspace-mcp/build.sh ship
# ╚══════════════════════════════════════════════════════════════════╝
# Type A — own code, arch=arm64
FROM python:3.11-slim
RUN apt-get update && apt-get install -y --no-install-recommends curl && rm -rf /var/lib/apt/lists/*
COPY uv run main.py --transport streamable-http --tool-tier complete /usr/local/bin/uv run main.py --transport streamable-http --tool-tier complete
LABEL org.opencontainers.image.source="https://github.com/diegonmarcos/cloud"
CMD ["uv run main.py --transport streamable-http --tool-tier complete"]
