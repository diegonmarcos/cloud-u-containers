# Gitea - Self-hosted Git server for code mirrors
# Deploy to: oci-p-flex_1

services:
  gitea:
    image: gitea/gitea:latest
    container_name: gitea
    restart: unless-stopped
    environment:
      - USER_UID=1000
      - USER_GID=1000
      - GITEA__database__DB_TYPE=sqlite3
      - GITEA__database__PATH=/data/gitea/gitea.db
      - GITEA__server__ROOT_URL=http://144.24.196.72:3000
      - GITEA__server__SSH_PORT=2222
      - GITEA__server__SSH_DOMAIN=144.24.196.72
      - GITEA__mirror__ENABLED=true
      - GITEA__mirror__DEFAULT_INTERVAL=1h
    volumes:
      - gitea_data:/data
      - /etc/timezone:/etc/timezone:ro
      - /etc/localtime:/etc/localtime:ro
    ports:
      - "3000:3000"    # Web UI
      - "2222:22"      # SSH for git
    networks:
      - backup_network

volumes:
  gitea_data:
    driver: local
    driver_opts:
      type: none
      o: bind
      device: /backup/code/gitea-data

networks:
  backup_network:
    driver: bridge
