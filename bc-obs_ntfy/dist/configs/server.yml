# ╔══════════════════════════════════════════════════════════════════╗
# ║ DO NOT EDIT — DECLARATIVE ENVIRONMENT — NIX FLAKES WAY           ║
# ║ AUTO-GENERATED by _shared/engine.nix — dist/ output is a build   ║
# ║ artifact. Edit src/ + run build.sh, never touch dist/.           ║
# ╠══════════════════════════════════════════════════════════════════╣
# ║ Source:  a_solutions/bc-obs_ntfy/src/flake.nix
# ║ Rebuild: a_solutions/bc-obs_ntfy/build.sh ship
# ╚══════════════════════════════════════════════════════════════════╝
# ntfy server configuration (auth enabled)
base-url: https://rss.diegonmarcos.com

# Cache and retention
cache-file: /var/cache/ntfy/cache.db
cache-duration: 720h
attachment-cache-dir: /var/cache/ntfy/attachments

# Limits
visitor-request-limit-burst: 60
visitor-request-limit-replenish: 10s
visitor-message-daily-limit: 0

# Auth — admin API access (Caddy injects credentials, anonymous r/w preserved)
auth-file: /var/cache/ntfy/auth.db
auth-default-access: read-write

# Web interface
enable-login: true
enable-signup: false
enable-reservations: false

# Listen on non-default port (host networking — port 80 is Caddy's)
listen-http: :8090

# Behind reverse proxy (Authelia handles auth)
behind-proxy: true
