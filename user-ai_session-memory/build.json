{
  "name": "session-memory",
  "description": "Central SQLite store of all Claude Code session transcripts — push/list/fetch so a session started on one device can be resumed from any other. WG-only sidecar on oci-apps; Node 22 built-in node:sqlite (phone-portable, no native deps).",
  "dns": "session-memory.app",
  "ports": {
    "app": 3108
  },
  "category": "agi",
  "docker": {
    "image": "session-memory",
    "dockerfile": "Dockerfile",
    "registry": "ghcr.io/diegonmarcos",
    "platform": "linux/arm64",
    "arch": "arm64",
    "native_build": {
      "type": "image-wrapper",
      "user": "appuser",
      "apt": "curl ca-certificates",
      "cmd": "curl -fsSL https://deb.nodesource.com/setup_22.x | bash - && apt-get install -y --no-install-recommends nodejs && npm install --omit=dev && useradd -r -m -u 10001 appuser && mkdir -p /data && chown -R appuser:appuser /data",
      "entrypoint": "node --experimental-sqlite server.mjs",
      "base_image": "debian:bookworm-slim",
      "app_dir": "code"
    }
  },
  "deploy": {
    "host": "oci-apps",
    "remote_path": "/opt/containers/session-memory",
    "compose_flags": "--build"
  },
  "proxy": {
    "app_hub": false
  },
  "containers": {
    "app": {
      "container_name": "session-memory",
      "image": "ghcr.io/diegonmarcos/session-memory:latest",
      "port": 3108,
      "port_env": "MEM_PORT",
      "dns": "session-memory.app",
      "public": false,
      "healthcheck": "/health",
      "monitoring": null,
      "env_file": false,
      "depends_on": [],
      "resources": null,
      "read_only": false,
      "protocol": "http"
    }
  }
}
