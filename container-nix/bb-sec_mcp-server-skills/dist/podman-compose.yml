services:
  mcp-server:
    build: .
    container_name: cloud-infra-mcp
    image: cloud-infra-mcp:latest
    stdin_open: true
    environment:
      - HOME=${HOME}
    volumes:
      - ${HOME}/git:${HOME}/git:ro
      - ${HOME}/.ssh:${HOME}/.ssh:ro
