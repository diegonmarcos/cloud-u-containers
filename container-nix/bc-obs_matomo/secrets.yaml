# Matomo secrets - ENCRYPT WITH: sops -e -i secrets.yaml

# MariaDB (matomo-db)
MYSQL_PASSWORD: CHANGE_ME_DB_PASSWORD
MYSQL_ROOT_PASSWORD: CHANGE_ME_ROOT_PASSWORD

# Matomo app (same password as MYSQL_PASSWORD)
MATOMO_DATABASE_PASSWORD: CHANGE_ME_DB_PASSWORD
