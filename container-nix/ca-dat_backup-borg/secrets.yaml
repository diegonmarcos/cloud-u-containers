# Borg backup secrets - ENCRYPT WITH: sops -e -i secrets.yaml

# Borg repository passphrase (IMPORTANT: save this somewhere safe!)
# Generate with: openssl rand -base64 32
BORG_PASSPHRASE: CHANGE_ME_BORG_PASSPHRASE

# Source directories to backup (space-separated)
SOURCE_DIRS: "/data/photos /data/syncthing /data/calendar"

# Archive name prefix
ARCHIVE_NAME: media
