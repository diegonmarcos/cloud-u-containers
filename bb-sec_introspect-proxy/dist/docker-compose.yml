# ╔══════════════════════════════════════════════════════════════════╗
# ║ DO NOT EDIT — DECLARATIVE ENVIRONMENT — NIX FLAKES WAY         ║
# ║ AUTO-GENERATED — DONT USE IMPERATIVE SOLUTIONS!!!              ║
# ╠══════════════════════════════════════════════════════════════════╣
# ║ Source: ~/git/cloud/a_solutions/bb-sec_introspect-proxy/src/flake.nix
# ║ Rebuild: build.sh ship
# ╚══════════════════════════════════════════════════════════════════╝
services:
  introspect-proxy:
    image: ghcr.io/diegonmarcos/introspect-proxy:latest
    container_name: introspect-proxy
    network_mode: host
    restart: unless-stopped
    stop_grace_period: 30s
    ulimits:
      nofile:
        soft: 65536
        hard: 65536
    dns:
      - 10.0.0.1
      - 1.1.1.1
    env_file:
      - .secrets
    healthcheck:
      test: ['CMD', 'curl', '-sf', 'http://localhost:4182/health']
      interval: 30s
      timeout: 10s
      retries: 3
    pids_limit: 256
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
    security_opt:
      - no-new-privileges:true
