# ╔══════════════════════════════════════════════════════════════════╗
# ║ DO NOT EDIT — DECLARATIVE ENVIRONMENT — NIX FLAKES WAY           ║
# ║ AUTO-GENERATED by _shared/engine.nix — dist/ output is a build   ║
# ║ artifact. Edit src/ + run build.sh, never touch dist/.           ║
# ╠══════════════════════════════════════════════════════════════════╣
# ║ Source:  a_solutions/aa-sui_matrix-mautrix-whatsapp/src/flake.nix
# ║ Rebuild: a_solutions/aa-sui_matrix-mautrix-whatsapp/build.sh ship
# ╚══════════════════════════════════════════════════════════════════╝
# Appservice registration for the Continuwuity homeserver. as_token / hs_token
# are ${PLACEHOLDERS} injected from .secrets by the deploy pre_hook. After
# deploy, register this (rendered) file once via the homeserver admin room:
#   !admin appservices register   (paste the rendered registration.yaml body)
id: whatsapp
url: http://10.0.0.6:29318
as_token: "${AS_TOKEN}"
hs_token: "${HS_TOKEN}"
sender_localpart: _bot_whatsappbot
rate_limited: false
namespaces:
    users:
        - regex: '^@whatsappbot:diegonmarcos.com$'
          exclusive: true
        - regex: '^@whatsapp_.*:diegonmarcos.com$'
          exclusive: true
    aliases:
        - regex: '^#whatsapp_.*:diegonmarcos.com$'
          exclusive: true
