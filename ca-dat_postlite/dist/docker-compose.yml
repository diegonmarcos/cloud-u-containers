# DO NOT EDIT - DECLARATIVE ENVIRONMENT - NIX FLAKES WAY
# AUTO-GENERATED - DONT USE IMPERATIVE SOLUTIONS!!!
# Source : ~/git/cloud/a_solutions/ca-dat_postlite/src/flake.nix
# Rebuild: ~/git/cloud/a_solutions/ca-dat_postlite/build.sh ship
# SQLite PostgreSQL wire-protocol proxies (postlite)
# Deployed on: gcp-E2-f_0 - bind_host=10.0.0.1 (WireGuard only)
# Tier-3: NO bare ":PORT" listeners; all bound to 10.0.0.1.

services:
  sqlite-npm:
    image: ghcr.io/diegonmarcos/postlite:latest
    container_name: sqlite-npm
    restart: "no"  # container-init handles startup
    network_mode: host
    volumes:
      - /home/diego/npm/data:/data
    command: ["-addr", "10.0.0.1:8890", "-data-dir", "/data"]

  sqlite-vaultwarden:
    image: ghcr.io/diegonmarcos/postlite:latest
    container_name: sqlite-vaultwarden
    restart: "no"  # container-init handles startup
    network_mode: host
    volumes:
      - /home/diego/vaultwarden/data:/data
    command: ["-addr", "10.0.0.1:8891", "-data-dir", "/data"]

  sqlite-ntfy:
    image: ghcr.io/diegonmarcos/postlite:latest
    container_name: sqlite-ntfy
    restart: "no"  # container-init handles startup
    network_mode: host
    volumes:
      - /home/diego/ntfy/cache:/data
    command: ["-addr", "10.0.0.1:8892", "-data-dir", "/data"]

  sqlite-authelia:
    image: ghcr.io/diegonmarcos/postlite:latest
    container_name: sqlite-authelia
    restart: "no"  # container-init handles startup
    network_mode: host
    volumes:
      - /home/diego/authelia/config:/data
    command: ["-addr", "10.0.0.1:8893", "-data-dir", "/data"]

  postlite-npm:
    image: ghcr.io/diegonmarcos/postlite:latest
    container_name: postlite-npm
    restart: "no"  # container-init handles startup
    network_mode: host
    volumes:
      - /home/diego/npm/data:/data
    command: ["-addr", "10.0.0.1:5433", "-data-dir", "/data"]

  postlite-vaultwarden:
    image: ghcr.io/diegonmarcos/postlite:latest
    container_name: postlite-vaultwarden
    restart: "no"  # container-init handles startup
    network_mode: host
    volumes:
      - /home/diego/vaultwarden/data:/data
    command: ["-addr", "10.0.0.1:5434", "-data-dir", "/data"]

  postlite-ntfy:
    image: ghcr.io/diegonmarcos/postlite:latest
    container_name: postlite-ntfy
    restart: "no"  # container-init handles startup
    network_mode: host
    volumes:
      - /home/diego/ntfy/cache:/data
    command: ["-addr", "10.0.0.1:5435", "-data-dir", "/data"]

  postlite-authelia:
    image: ghcr.io/diegonmarcos/postlite:latest
    container_name: postlite-authelia
    restart: "no"  # container-init handles startup
    network_mode: host
    volumes:
      - /home/diego/authelia/config:/data
    command: ["-addr", "10.0.0.1:5436", "-data-dir", "/data"]

