# ╔══════════════════════════════════════════════════════════════════╗
# ║ Cloud Builder — x-deb-nixhm                                      ║
# ║ DO NOT EDIT — generated by cb_containers-builders/src/flake.nix ║
# ║ Rebuild: build.sh build                                         ║
# ╠══════════════════════════════════════════════════════════════════╣
# ║ Usage:                                                          ║
# ║   docker compose -f compose-x-deb-nixhm.yaml run builder cmd     ║
# ╚══════════════════════════════════════════════════════════════════╝

services:
  builder:
    image: ghcr.io/diegonmarcos/cloud-builder-x-deb-nixhm:latest
    build:
      context: .
      dockerfile: Dockerfile.x-deb-nixhm
      network: host
    container_name: cloud-builder-x-deb-nixhm
    platform: linux/amd64,linux/arm64
    network_mode: host
    privileged: true
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - ${HOME:-/root}/.ssh:/root/.ssh:ro
      - ${HOME:-/root}/.config/sops:/root/.config/sops:ro
    working_dir: /root/git/cloud
    entrypoint: ["/opt/cloud-builder/entrypoint.sh"]
    environment:
      - TERM=xterm-256color
      - GITHUB_TOKEN=${GITHUB_TOKEN:-}
      - GITHUB_ACTOR=${GITHUB_ACTOR:-}
    labels:
      diego.managed-by: "build.sh"
      diego.image.variant: "x-deb-nixhm"
