# ╔══════════════════════════════════════════════════════════════════╗
# ║ DO NOT EDIT — DECLARATIVE ENVIRONMENT — NIX FLAKES WAY         ║
# ║ AUTO-GENERATED — DONT USE IMPERATIVE SOLUTIONS!!!              ║
# ╠══════════════════════════════════════════════════════════════════╣
# ║ Source: ~/git/cloud-infra/a_solutions/bc-obs_fluent-bit/src/flake.nix ║
# ║ Rebuild: ~/git/cloud-infra/a_solutions/bc-obs_fluent-bit/build.sh ship ║
# ╚══════════════════════════════════════════════════════════════════╝
services:
  fluent-bit:
    image: ghcr.io/diegonmarcos/fluent-bit:latest
    build:
      context: .
      dockerfile_inline: |
        FROM fluent/fluent-bit:latest
        COPY docker-compose.yml /opt/cloud-docs/docker-compose.yml
        LABEL org.opencontainers.image.source="https://github.com/diegonmarcos/cloud-infra"
        LABEL org.opencontainers.image.description="fluent-bit"
        LABEL org.opencontainers.image.title="fluent-bit"
        LABEL cloud.diegonmarcos.from-image="fluent/fluent-bit:latest"
        LABEL cloud.diegonmarcos.registry="ghcr.io/diegonmarcos"
    container_name: fluent-bit
    restart: "no"  # container-init handles startup
    network_mode: host
    volumes:
      - /opt/fluent-bit/fluent-bit.conf:/fluent-bit/etc/fluent-bit.conf:ro
      - /opt/fluent-bit/parsers.conf:/fluent-bit/etc/parsers.conf:ro
      - /var/lib/docker/containers:/var/lib/docker/containers:ro
      - /var/log:/var/log:ro
      - fluent-bit-db:/fluent-bit/db

volumes:
  fluent-bit-db:
    driver: local
