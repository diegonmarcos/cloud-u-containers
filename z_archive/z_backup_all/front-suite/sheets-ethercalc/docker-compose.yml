# Grist - Modern collaborative spreadsheet
# Alternative to Google Sheets (replaces unmaintained Ethercalc)
# Deployed on: oci-p-flex_1 (Oracle Flex)

services:
  grist:
    image: gristlabs/grist:latest
    container_name: grist_app
    restart: "no"

    ports:
      - "3011:8484"

    volumes:
      - grist_data:/persist

    environment:
      - GRIST_SESSION_SECRET=changeme-secret-key
      - GRIST_SINGLE_ORG=docs
      - GRIST_DEFAULT_EMAIL=${GRIST_ADMIN_EMAIL:-admin@diegonmarcos.com}
      - APP_HOME_URL=https://sheets.diegonmarcos.com
      - GRIST_SANDBOX_FLAVOR=unsandboxed

    networks:
      - dev_network

    healthcheck:
      test: ['CMD', 'curl', '-f', 'http://localhost:8484/']
      interval: 30s
      timeout: 10s
      retries: 3

networks:
  dev_network:
    external: true
    name: dev_network

volumes:
  grist_data:
    driver: local
