schedule: "*/5 * * * *"
env:
  - AUTHELIA_BEARER_TOKEN: ${AUTHELIA_BEARER_TOKEN}
  - C3_API: http://10.0.0.6:8081
  - REPO_DIR: /var/lib/dagu/data/cloud-data
  - REPO_URL: git@github.com:diegonmarcos/cloud-data.git
  - NTFY_URL: http://10.0.0.1:8090
mailOn:
  failure: true
  success: false
steps:
  - name: ensure-clone
    command: |
      export GIT_SSH_COMMAND="ssh -i /root/.ssh/vault_id_rsa -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null -o LogLevel=ERROR"
      if [ ! -d "$REPO_DIR/.git" ]; then
        git clone "$REPO_URL" "$REPO_DIR"
        cd "$REPO_DIR"
        git config user.name "dagu-bot"
        git config user.email "no-reply@diegonmarcos.com"
      fi
  - name: pull-rebase
    depends:
      - ensure-clone
    command: |
      export GIT_SSH_COMMAND="ssh -i /root/.ssh/vault_id_rsa -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null -o LogLevel=ERROR"
      cd "$REPO_DIR"
      # Clean stale git locks from crashed processes
      rm -f .git/index.lock .git/shallow.lock .git/refs/heads/*.lock 2>/dev/null
      git pull --rebase origin main || {
        echo "Rebase conflict — resetting to remote"
        git rebase --abort 2>/dev/null
        git reset --hard origin/main
      }
  - name: fetch-cloud-data-topology
    depends:
      - pull-rebase
    command: |
      if curl -sf "$C3_API/cloud-data/topology" | jq '.' > "$REPO_DIR/cloud-data-topology.json.tmp" && [ -s "$REPO_DIR/cloud-data-topology.json.tmp" ]; then
        mv "$REPO_DIR/cloud-data-topology.json.tmp" "$REPO_DIR/cloud-data-topology.json"
        echo "Fetched cloud-data-topology.json ($(wc -c < "$REPO_DIR/cloud-data-topology.json") bytes)"
      else
        rm -f "$REPO_DIR/cloud-data-topology.json.tmp"
        echo "SKIP cloud-data-topology.json (API returned empty or failed)"
      fi
  - name: fetch-cloud-data-configs
    depends:
      - pull-rebase
    command: |
      if curl -sf "$C3_API/cloud-data/configs" | jq '.' > "$REPO_DIR/cloud-data-configs.json.tmp" && [ -s "$REPO_DIR/cloud-data-configs.json.tmp" ]; then
        mv "$REPO_DIR/cloud-data-configs.json.tmp" "$REPO_DIR/cloud-data-configs.json"
        echo "Fetched cloud-data-configs.json ($(wc -c < "$REPO_DIR/cloud-data-configs.json") bytes)"
      else
        rm -f "$REPO_DIR/cloud-data-configs.json.tmp"
        echo "SKIP cloud-data-configs.json (API returned empty or failed)"
      fi
  - name: fetch-cloud-data-deps
    depends:
      - pull-rebase
    command: |
      if curl -sf "$C3_API/cloud-data/deps" | jq '.' > "$REPO_DIR/cloud-data-deps.json.tmp" && [ -s "$REPO_DIR/cloud-data-deps.json.tmp" ]; then
        mv "$REPO_DIR/cloud-data-deps.json.tmp" "$REPO_DIR/cloud-data-deps.json"
        echo "Fetched cloud-data-deps.json ($(wc -c < "$REPO_DIR/cloud-data-deps.json") bytes)"
      else
        rm -f "$REPO_DIR/cloud-data-deps.json.tmp"
        echo "SKIP cloud-data-deps.json (API returned empty or failed)"
      fi
  - name: fetch-cloud-data-dns-services
    depends:
      - pull-rebase
    command: |
      if curl -sf "$C3_API/cloud-data/dns-services" | jq '.' > "$REPO_DIR/cloud-data-dns-services.json.tmp" && [ -s "$REPO_DIR/cloud-data-dns-services.json.tmp" ]; then
        mv "$REPO_DIR/cloud-data-dns-services.json.tmp" "$REPO_DIR/cloud-data-dns-services.json"
        echo "Fetched cloud-data-dns-services.json ($(wc -c < "$REPO_DIR/cloud-data-dns-services.json") bytes)"
      else
        rm -f "$REPO_DIR/cloud-data-dns-services.json.tmp"
        echo "SKIP cloud-data-dns-services.json (API returned empty or failed)"
      fi
  - name: fetch-cloud-data-caddy-routes
    depends:
      - pull-rebase
    command: |
      if curl -sf "$C3_API/cloud-data/caddy-routes" | jq '.' > "$REPO_DIR/cloud-data-caddy-routes.json.tmp" && [ -s "$REPO_DIR/cloud-data-caddy-routes.json.tmp" ]; then
        mv "$REPO_DIR/cloud-data-caddy-routes.json.tmp" "$REPO_DIR/cloud-data-caddy-routes.json"
        echo "Fetched cloud-data-caddy-routes.json ($(wc -c < "$REPO_DIR/cloud-data-caddy-routes.json") bytes)"
      else
        rm -f "$REPO_DIR/cloud-data-caddy-routes.json.tmp"
        echo "SKIP cloud-data-caddy-routes.json (API returned empty or failed)"
      fi
  - name: fetch-cloud-data-authelia-acl
    depends:
      - pull-rebase
    command: |
      if curl -sf "$C3_API/cloud-data/authelia-acl" | jq '.' > "$REPO_DIR/cloud-data-authelia-acl.json.tmp" && [ -s "$REPO_DIR/cloud-data-authelia-acl.json.tmp" ]; then
        mv "$REPO_DIR/cloud-data-authelia-acl.json.tmp" "$REPO_DIR/cloud-data-authelia-acl.json"
        echo "Fetched cloud-data-authelia-acl.json ($(wc -c < "$REPO_DIR/cloud-data-authelia-acl.json") bytes)"
      else
        rm -f "$REPO_DIR/cloud-data-authelia-acl.json.tmp"
        echo "SKIP cloud-data-authelia-acl.json (API returned empty or failed)"
      fi
  - name: fetch-cloud-data-monitoring-targets
    depends:
      - pull-rebase
    command: |
      if curl -sf "$C3_API/cloud-data/monitoring-targets" | jq '.' > "$REPO_DIR/cloud-data-monitoring-targets.json.tmp" && [ -s "$REPO_DIR/cloud-data-monitoring-targets.json.tmp" ]; then
        mv "$REPO_DIR/cloud-data-monitoring-targets.json.tmp" "$REPO_DIR/cloud-data-monitoring-targets.json"
        echo "Fetched cloud-data-monitoring-targets.json ($(wc -c < "$REPO_DIR/cloud-data-monitoring-targets.json") bytes)"
      else
        rm -f "$REPO_DIR/cloud-data-monitoring-targets.json.tmp"
        echo "SKIP cloud-data-monitoring-targets.json (API returned empty or failed)"
      fi
  - name: fetch-cloud-data-firewall-rules
    depends:
      - pull-rebase
    command: |
      if curl -sf "$C3_API/cloud-data/firewall-rules" | jq '.' > "$REPO_DIR/cloud-data-firewall-rules.json.tmp" && [ -s "$REPO_DIR/cloud-data-firewall-rules.json.tmp" ]; then
        mv "$REPO_DIR/cloud-data-firewall-rules.json.tmp" "$REPO_DIR/cloud-data-firewall-rules.json"
        echo "Fetched cloud-data-firewall-rules.json ($(wc -c < "$REPO_DIR/cloud-data-firewall-rules.json") bytes)"
      else
        rm -f "$REPO_DIR/cloud-data-firewall-rules.json.tmp"
        echo "SKIP cloud-data-firewall-rules.json (API returned empty or failed)"
      fi
  - name: fetch-cloud-data-backup-targets
    depends:
      - pull-rebase
    command: |
      if curl -sf "$C3_API/cloud-data/backup-targets" | jq '.' > "$REPO_DIR/cloud-data-backup-targets.json.tmp" && [ -s "$REPO_DIR/cloud-data-backup-targets.json.tmp" ]; then
        mv "$REPO_DIR/cloud-data-backup-targets.json.tmp" "$REPO_DIR/cloud-data-backup-targets.json"
        echo "Fetched cloud-data-backup-targets.json ($(wc -c < "$REPO_DIR/cloud-data-backup-targets.json") bytes)"
      else
        rm -f "$REPO_DIR/cloud-data-backup-targets.json.tmp"
        echo "SKIP cloud-data-backup-targets.json (API returned empty or failed)"
      fi
  - name: fetch-cloud-data-wireguard-peers
    depends:
      - pull-rebase
    command: |
      if curl -sf "$C3_API/cloud-data/wireguard-peers" | jq '.' > "$REPO_DIR/cloud-data-wireguard-peers.json.tmp" && [ -s "$REPO_DIR/cloud-data-wireguard-peers.json.tmp" ]; then
        mv "$REPO_DIR/cloud-data-wireguard-peers.json.tmp" "$REPO_DIR/cloud-data-wireguard-peers.json"
        echo "Fetched cloud-data-wireguard-peers.json ($(wc -c < "$REPO_DIR/cloud-data-wireguard-peers.json") bytes)"
      else
        rm -f "$REPO_DIR/cloud-data-wireguard-peers.json.tmp"
        echo "SKIP cloud-data-wireguard-peers.json (API returned empty or failed)"
      fi
  - name: fetch-cloud-data-ntfy-acl
    depends:
      - pull-rebase
    command: |
      if curl -sf "$C3_API/cloud-data/ntfy-acl" | jq '.' > "$REPO_DIR/cloud-data-ntfy-acl.json.tmp" && [ -s "$REPO_DIR/cloud-data-ntfy-acl.json.tmp" ]; then
        mv "$REPO_DIR/cloud-data-ntfy-acl.json.tmp" "$REPO_DIR/cloud-data-ntfy-acl.json"
        echo "Fetched cloud-data-ntfy-acl.json ($(wc -c < "$REPO_DIR/cloud-data-ntfy-acl.json") bytes)"
      else
        rm -f "$REPO_DIR/cloud-data-ntfy-acl.json.tmp"
        echo "SKIP cloud-data-ntfy-acl.json (API returned empty or failed)"
      fi
  - name: fetch-cloud-data-cloudflare-dns
    depends:
      - pull-rebase
    command: |
      if curl -sf "$C3_API/cloud-data/cloudflare-dns" | jq '.' > "$REPO_DIR/cloud-data-cloudflare-dns.json.tmp" && [ -s "$REPO_DIR/cloud-data-cloudflare-dns.json.tmp" ]; then
        mv "$REPO_DIR/cloud-data-cloudflare-dns.json.tmp" "$REPO_DIR/cloud-data-cloudflare-dns.json"
        echo "Fetched cloud-data-cloudflare-dns.json ($(wc -c < "$REPO_DIR/cloud-data-cloudflare-dns.json") bytes)"
      else
        rm -f "$REPO_DIR/cloud-data-cloudflare-dns.json.tmp"
        echo "SKIP cloud-data-cloudflare-dns.json (API returned empty or failed)"
      fi
  - name: fetch-cloud-data-matomo-sites
    depends:
      - pull-rebase
    command: |
      if curl -sf "$C3_API/cloud-data/matomo-sites" | jq '.' > "$REPO_DIR/cloud-data-matomo-sites.json.tmp" && [ -s "$REPO_DIR/cloud-data-matomo-sites.json.tmp" ]; then
        mv "$REPO_DIR/cloud-data-matomo-sites.json.tmp" "$REPO_DIR/cloud-data-matomo-sites.json"
        echo "Fetched cloud-data-matomo-sites.json ($(wc -c < "$REPO_DIR/cloud-data-matomo-sites.json") bytes)"
      else
        rm -f "$REPO_DIR/cloud-data-matomo-sites.json.tmp"
        echo "SKIP cloud-data-matomo-sites.json (API returned empty or failed)"
      fi
  - name: fetch-cloud-data-container-resources
    depends:
      - pull-rebase
    command: |
      if curl -sf "$C3_API/cloud-data/container-resources" | jq '.' > "$REPO_DIR/cloud-data-container-resources.json.tmp" && [ -s "$REPO_DIR/cloud-data-container-resources.json.tmp" ]; then
        mv "$REPO_DIR/cloud-data-container-resources.json.tmp" "$REPO_DIR/cloud-data-container-resources.json"
        echo "Fetched cloud-data-container-resources.json ($(wc -c < "$REPO_DIR/cloud-data-container-resources.json") bytes)"
      else
        rm -f "$REPO_DIR/cloud-data-container-resources.json.tmp"
        echo "SKIP cloud-data-container-resources.json (API returned empty or failed)"
      fi
  - name: fetch-cloud-data-log-routing
    depends:
      - pull-rebase
    command: |
      if curl -sf "$C3_API/cloud-data/log-routing" | jq '.' > "$REPO_DIR/cloud-data-log-routing.json.tmp" && [ -s "$REPO_DIR/cloud-data-log-routing.json.tmp" ]; then
        mv "$REPO_DIR/cloud-data-log-routing.json.tmp" "$REPO_DIR/cloud-data-log-routing.json"
        echo "Fetched cloud-data-log-routing.json ($(wc -c < "$REPO_DIR/cloud-data-log-routing.json") bytes)"
      else
        rm -f "$REPO_DIR/cloud-data-log-routing.json.tmp"
        echo "SKIP cloud-data-log-routing.json (API returned empty or failed)"
      fi
  - name: fetch-cloud-data-gha-config
    depends:
      - pull-rebase
    command: |
      if curl -sf "$C3_API/cloud-data/gha-config" | jq '.' > "$REPO_DIR/cloud-data-gha-config.json.tmp" && [ -s "$REPO_DIR/cloud-data-gha-config.json.tmp" ]; then
        mv "$REPO_DIR/cloud-data-gha-config.json.tmp" "$REPO_DIR/cloud-data-gha-config.json"
        echo "Fetched cloud-data-gha-config.json ($(wc -c < "$REPO_DIR/cloud-data-gha-config.json") bytes)"
      else
        rm -f "$REPO_DIR/cloud-data-gha-config.json.tmp"
        echo "SKIP cloud-data-gha-config.json (API returned empty or failed)"
      fi
  - name: commit-and-push
    depends:
      - fetch-cloud-data-topology
      - fetch-cloud-data-configs
      - fetch-cloud-data-deps
      - fetch-cloud-data-dns-services
      - fetch-cloud-data-caddy-routes
      - fetch-cloud-data-authelia-acl
      - fetch-cloud-data-monitoring-targets
      - fetch-cloud-data-firewall-rules
      - fetch-cloud-data-backup-targets
      - fetch-cloud-data-wireguard-peers
      - fetch-cloud-data-ntfy-acl
      - fetch-cloud-data-cloudflare-dns
      - fetch-cloud-data-matomo-sites
      - fetch-cloud-data-container-resources
      - fetch-cloud-data-log-routing
      - fetch-cloud-data-gha-config
    command: |
      export GIT_SSH_COMMAND="ssh -i /root/.ssh/vault_id_rsa -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null -o LogLevel=ERROR"
      cd "$REPO_DIR"
      ls -1 *.json 2>/dev/null | grep -v manifest.json | sed 's/\.json$//' | \
        awk '{name=$0; gsub(/-/," ",name); for(i=1;i<=split(name,a," ");i++) a[i]=toupper(substr(a[i],1,1)) substr(a[i],2); name=""; for(i=1;i<=length(a);i++) name=name (i>1?" ":"") a[i]; printf "{\"file\":\"%s.json\",\"name\":\"%s\"}\n",$0,name}' | \
        jq -s '.' > manifest.json
      git add -A *.json manifest.json
      if git diff --cached --quiet; then
        echo "No changes — skipping commit"
        exit 0
      fi
      STAMP=$(date -u +"%Y-%m-%dT%H:%M:%SZ")
      git commit -m "sync: cloud data ${STAMP}"
      git push origin main
      echo "Pushed changes at ${STAMP}"
