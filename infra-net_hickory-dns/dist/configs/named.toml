# ╔══════════════════════════════════════════════════════════════════╗
# ║ DO NOT EDIT — DECLARATIVE ENVIRONMENT — NIX FLAKES WAY           ║
# ║ AUTO-GENERATED by _shared/engine.nix — dist/ output is a build   ║
# ║ artifact. Edit src/ + run build.sh, never touch dist/.           ║
# ╠══════════════════════════════════════════════════════════════════╣
# ║ Source:  a_solutions/infra-net_hickory-dns/src/flake.nix
# ║ Rebuild: a_solutions/infra-net_hickory-dns/build.sh ship
# ╚══════════════════════════════════════════════════════════════════╝
listen_addrs_ipv4 = ["10.0.0.1"]
# wg0 mesh ULA — serve DNS over IPv6 too (fd0c:1d00::1). Bind "::" (all v6)
# not the specific ULA, so a not-yet-up wg0 addr can't crash-loop the resolver.
# gcp-proxy has no public IPv6, so this is reachable only over wg0.
listen_addrs_ipv6 = ["::"]
listen_port = 53
directory = "/etc/zones"

# ── Wildcard zones (catch-all → Caddy WG IP) ──
[[zones]]
zone = "app"
zone_type = "Primary"
file = "app.zone"


[[zones]]
zone = "db"
zone_type = "Primary"
file = "db.zone"


[[zones]]
zone = "diegonmarcos.com"
zone_type = "Primary"
file = "diegonmarcos.com.zone"


# ── Root forwarder (everything else → declared upstream DNS) ──
[[zones]]
zone = "."
zone_type = "External"

[zones.stores]
type = "forward"

[[zones.stores.name_servers]]
ip = "1.1.1.1"
connections = [
  { protocol = { type = "udp" } },
  { protocol = { type = "tcp" } }
]


[[zones.stores.name_servers]]
ip = "8.8.8.8"
connections = [
  { protocol = { type = "udp" } },
  { protocol = { type = "tcp" } }
]

