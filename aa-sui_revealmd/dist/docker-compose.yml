# ╔══════════════════════════════════════════════════════════════════╗
# ║ DO NOT EDIT — DECLARATIVE ENVIRONMENT — NIX FLAKES WAY         ║
# ║ AUTO-GENERATED — DONT USE IMPERATIVE SOLUTIONS!!!              ║
# ╠══════════════════════════════════════════════════════════════════╣
# ║ Source: ~/git/cloud/a_solutions/aa-sui_revealmd/src/flake.nix   ║
# ║ Rebuild: ~/git/cloud/a_solutions/aa-sui_revealmd/build.sh ship  ║
# ╚══════════════════════════════════════════════════════════════════╝
# RevealMD - Markdown to reveal.js presentations
# Deployed on: oci-A1-f_1 (Oracle Flex)

services:
  revealmd:
    image: webpronl/reveal-md:latest
    container_name: revealmd_app
    restart: "no"  # container-init handles startup
    network_mode: host
    volumes:
      - slides_data:/slides
    command: /slides --watch --port 3014
    healthcheck:
      test: ['CMD', 'wget', '-q', '--spider', 'http://localhost:3014']
      interval: 30s
      timeout: 10s
      retries: 3

volumes:
  slides_data:
    driver: local
