# Profile: Normal / Senior (Opus)

> `CLAUDE_MODE=normal CLAUDE_MODEL=opus`

## Skills Available

- Senior Cloud Architect
- Senior Software Engineer
- Senior Software Architecture
- Senior Front-End Developer
- Senior Designer
- Junior Software Engineer
- Junior Ops

## Enforcement Output

All 33 rules active at full strength.

### Critical BLOCK (always exit 2)

| # | Pattern | Message |
|---|---------|---------|
| 29 | `which` | Use `command -v` |
| 30 | `ssh.*echo.*>.*\.secrets` | Create `src/secrets.yaml` + sops |
| 10a | Edit `~/.claude/CLAUDE.md` | Edit source in `~/git/cloud-unix/` flakes |
| 10b | Edit `~/.mcp.json` | Edit source in `~/git/cloud-unix/` flakes |
| 10c | Edit `~/.claude/settings.json` | Edit source in `~/git/cloud-unix/` flakes |
| 11 | Edit `*/dist/*` | Edit source in `src/` |
| 12 | Edit `/nix/store/*` | Nix store is immutable |
| 13 | Edit root `front/package.json` | Auto-generated by `deploy.sh deps` |
| 16 | `style=""` in .html/.svelte/.vue | No inline CSS — use SCSS |
| 18 | `export let` in .svelte | Svelte 5 runes: `$props()` |
| 20 | `on:click` in .svelte | Svelte 5: `onclick={handler}` |

### Standard BLOCK (exit 2)

| # | Pattern | Message |
|---|---------|---------|
| 1 | `nix build\|run\|develop` | Use `./build.sh build` |
| 2 | `nixos-rebuild` | Use `build.sh switch` |
| 3 | `home-manager switch` | Use `build.sh switch` |
| 4 | `nix-on-droid switch` | Use `build.sh switch` |

### WARN (stderr, allow through)

| # | Pattern | Message |
|---|---------|---------|
| 5 | `npm run build\|dev` | Use `./build.sh` |
| 6 | `npm install` in subdir | Use `deploy.sh deps` |
| 7 | `ssh.*docker compose` | Use `build.sh ship` |
| 8 | `docker compose up` without `--env-file` | Missing `.secrets` |
| 23 | `rsync.*:/opt/containers` | Use `build.sh ship` |
| 24 | `nix-env -i` | Add to flake |
| 25 | `pip install` | Add to flake |
| 26 | `cargo install` | Add to flake |
| 27 | `npm install -g` | Add to flake |
| 28 | `apt install` | Use nix flakes |
| 31 | `ssh.*sed.*-i` | Edit Nix source instead |
| 32 | `scp.*:/opt/containers` | Use `build.sh ship` |
| 14 | Write to `secrets.yaml` | Use `sops -e` |
| 17 | `: any` in .ts | Define proper type |
| 19 | `$:` in .svelte | Use `$derived()` or `$effect()` |
| 21 | `data()\|methods:` in .vue | Use Composition API |
| 22 | `float: left\|right` in .scss | Use flexbox/grid |

### Junior Gate

Not active — Opus has full access.

## Allowed Operations

- `build.sh ship` (full deploy pipeline)
- VM lifecycle (`vm_start`, `vm_stop`, `vm_reset`)
- Architecture changes (new services, flake modifications)
- Secrets management (`sops -e`, `build.sh secrets`)
- All MCP tools (44 tools)
